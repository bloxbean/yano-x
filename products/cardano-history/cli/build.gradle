plugins {
    id 'application'
}

dependencies {
    implementation project(':products:cardano-history:client')
    implementation libs.jackson.databind

    testImplementation libs.junit.jupiter.api
    testRuntimeOnly libs.junit.jupiter.engine
    testRuntimeOnly libs.junit.platform.launcher
    testImplementation libs.assertj.core
}

application {
    mainClass = 'org.yanoproject.x.history.cli.CardanoHistoryCli'
    applicationName = 'yano-cardano-history'
}

java {
    sourceCompatibility = 25
    targetCompatibility = 25
}

tasks.named('compileJava') {
    options.compilerArgs.removeAll { it.startsWith('-Aproject=') }
}
