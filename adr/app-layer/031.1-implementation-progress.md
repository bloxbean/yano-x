# ADR-031.1 implementation evidence

This ledger records completed implementation and local acceptance evidence.
All gates below pass. The capability remains experimental; local qualification
does not publish the required upstream host artifacts or promote a public release.

## Workspaces

- Yano X: `feat/adr031-1-declarative-bindings` in the original checkout.
- Yano: `feat/adr031-1-transition-kernels` in
  `/Users/satya/work/bloxbean/yano-adr031-1`.
- Existing unrelated changes in both repositories are preserved.

## Milestones

| Milestone | Status | Evidence / remaining work |
|---|---|---|
| ADR expression update | Implemented | Restricted CEL, canonical IR, work accounting, product boundaries and resolved choices reconciled with independent reviews |
| Phase 0: host contracts | Implemented | Full core API, runtime and plugin-catalog test tasks pass; API level 10; exact pre17-SNAPSHOT Maven/ZIP development publication |
| Phase 1: contracts and foundation | Implemented | IR/profile routing, stock kernels, canonical/adversarial tests, published CDDL and frozen vectors pass; independent third-party catalog/lifecycle fixture passes |
| Phase 2: cascade and stock refactors | Implemented | Atomicity/overlay/replay/accounting tests, unchanged standalone/map vectors, pure role decisions and three-node cascades pass |
| Phase 3: expressions and evidence | Implemented | Restricted CEL, all stock-function vectors, evidence checks, shared crypto budgets and durable outbox intent tests pass |
| Phase 4: provider and packaging | Qualified | API10 catalog leaves, four real-recipe recovery/replay/proof/parity tests, governed cutover, distribution checks and complete packaged smoke pass |
| Phase 5: tooling and documentation | Qualified | Strict compiler, real-catalog and packaged CLI, blueprint digest locks, Studio graph, user/reference docs and Javadoc; aggregate gate passes |
| Final local qualification | Passed | Full source/integration/crypto/distribution gates, packaged smoke and independent review close; PR delivery does not publish host artifacts |

## Review observations

- Kernel access must traverse both the plugin callback facade and the
  startup wrapper; exposing it only on the raw machine loses discovery.
- Catalog-driven component construction needs a narrow host resolver in
  `AppStateMachineContext`; the original context has no provider registry.
- API level increases from 9 to 10 for the additive contracts.
- Legacy composite profiles must keep their original bytes, not merely
  decode successfully under a new codec.
- Raw-body mappings can select any opcode supported by the target codec.
  Evidence restrictions must inspect every target command, not just the named
  descriptor. A regression test covers this bypass.
- Receipt encoding belongs before component commits; receipt capacity must
  never turn an already committed cascade into a rejected receipt.
- Javadoc is part of each milestone: document public contracts, deterministic
  wire/accounting rules, authority boundaries, and infrastructure versus
  business-failure semantics. Do not describe unfinished behavior as guaranteed.
- Governed authorization needs explicit cross-participant read views. The kernel
  now declares read participants, and the engine snapshots their permissions and
  supplies only read-only overlays. Unknown, duplicate and self reads fail
  construction; plugin callback tests cover both facts overloads.
- A real-host registry -> approvals -> document-trail test passes, including
  receipt query/proof retrieval and identical root after persisted restart.
  This is single-node evidence, not completion of multi-node/replay qualification.
- Broad composition contracts/runtime, stdlib, and devtools suites pass after
  the participant-read extension. Focused tests pass again after the latest
  transition input validation and stock-adapter cleanup.
- Host Maven artifacts and matching JVM ZIP were rebuilt successfully after the
  participant-read API change using `0.1.0-pre17-SNAPSHOT`.

## Reviewed architecture extension

The user approved extending the ADR for the governed role-to-map path and
requested autonomous implementation with multiple independent reviewers. In
`ActorApprovalProcessor.apply`, `GovernedCryptoWork.reserve` writes shared
cryptographic-work accounting into actor state even when the command later
rejects. The current kernel can read declared foreign participants, but its
plan writes only its own component. Read access alone does not solve this.

Approved direction: explicitly bounded participant-owned, non-refundable work
accounting (not general foreign business plans), with unchanged standalone semantics and tests
for rejected cascades, quota exhaustion, replay and rollback. Independent
authority-boundary and integration reviews reconciled this design. Governed
role-to-map bindings pass the three-node qualification below. The upstream
change is [Yano PR #147](https://github.com/bloxbean/yano/pull/147).

### Review reconciliation and evidence (2026-09-23)

- Two reviewers agreed the actual role-to-map path needs a shared owner-declared
  work reservation, not general foreign business writes. The host now publishes
  typed budgets/references/requests, the plugin facade wraps them, and existing
  governed routes delegate to the same 12-byte counter helper.
- Host focused API/TCCL tests and exact local Maven/ZIP publication pass after
  both the accounting and logical-lookup callback additions.
- Cascades test retained charges after rejection, no charge on receipt replay,
  quota rejection before facts/signatures, and protected accounting keys.
- Role tests cover pure decision parity, staging, changed-action replay,
  terminal cleanup, empty-block expiry, invalid signatures and retained charges.
- Two real loopback-N2N three-node scenarios pass: member approval/audit and
  actor approval/map. They cover common roots/identity/capability digests,
  certified proofs, mismatched-action atomic rollback and one-use consumption;
  the member scenario also covers offline follower catch-up and full restart.
- Independent review reproduced a dense receipt encoder/decoder item-limit
  mismatch. The scanner now matches the enclosing 64-KiB wire cap and a dense
  receipt round-trip regression was added; broader rerun follows.
- Product scope is explicitly corrected in ADR §5.12 after two independent
  reviews: derived commands do not satisfy legacy signed-message certificate
  formats or envelope-replay projections. Qualification uses receipt/state proof
  export and actual product schemas/policies, without claiming old portal or
  certificate compatibility. This is not an unperformed product-owner approval.
- No retained deployment, keys, genesis identity, or preprod traffic was changed.

### Final qualification observations

- The full upstream `:core-api:test :runtime:test :plugin-catalog:test` gate
  passed in 10m08s. Existing opt-in/disabled tests remain skipped; this is not
  a Cardano mainnet/preprod sync run.
- The three-node governed profile test initially used ordinary submission for
  a reserved governance topic. It now uses the host's established privileged
  system-message API and passes activation-height, quorum/readiness, and restart
  assertions. Production authorization was not relaxed.
- All four checked-in recipes pass byte-for-byte live/dry-run receipt parity,
  follower outage/catch-up, caller-pinned certified receipt proofs, and full
  three-node restart. Procurement additionally retains its exact durable outbox
  intent; no external webhook delivery is implied.
- CDDL 0.10.5 validates six existing governance and four new binding vectors.
  The production Java codecs match the newly published frozen bytes.
- The aggregate run exposed a same-version local Julc compiler override, not a
  settlement source change. Two reviewers confirmed the published compiler SHA
  against `docs/BUILD_AND_TEST.md`. Maven Local is now limited to host group
  `org.yanoproject`; settlement templates and retained script identities are
  not regenerated. The aggregate gate passes with the corrected inputs.
- The independent third-party fixture was added while an aggregate Gradle
  invocation was already configured; that invocation lacked its new compiler
  classpath property. Fresh configured integration runs now pass all seven CLI
  cases, including independently compiled provider loading and lifetime checks.
- Final acceptance review added exact maximum-map-batch, old-receipt proof after
  cutover, cross-topic ordering, block charging, runtime-limit rollback,
  evidence-mapping, lookup overlay, and retained-IR marker assertions. Test-only
  import/diagnostic expectation corrections pass the final aggregate rerun.
  Maximum-batch parity compares matching command families: final-v1 kernel to
  final-v1 standalone, and legacy pure plan to legacy standalone, preserving all
  130 state keys and receipt bytes in each comparison and 129 kernel events.
- All four recipe integration cases also pass fresh-node replay from genesis:
  a new empty store reconstructs the authoritative root, receipts, outbox,
  identity/profile/manifest, and certified proofs from finalized blocks. This
  is separate evidence from retained restart and the root-free CLI rehearsal.
- Two reviewers reconciled structural versus runtime limits and domain-owned
  one-use approval semantics in the ADR. No generic item identifier or fictitious
  framework consumption token was added. Tests preserve exact receipt replay,
  terminal approval no-ops, failed-consequence rollback, and map consumption.
- The full source/integration/crypto run completed with only two packaging
  failures: duplicate host/X devnet overlay and missing offline POM license
  metadata. The X overlay now owns its explicit paths. Online CycloneDX metadata
  resolution passes without new license overrides; CEL uses only its compiler
  module, not its runtime aggregator. The rebuilt distribution verifies.

### Packaged smoke evidence (2026-09-23)

The unmodified release smoke script passed on Darwin 25.0.0 arm64 / Java 25,
using a frozen copy of `yano-x-jvm-0.1.0-pre2.zip` (514,454,662 bytes).

- Yano Maven identity: `0.1.0-pre17-SNAPSHOT` (local development publication).
- Matching host ZIP: `/Users/satya/work/bloxbean/yano-adr031-1/app/build/distributions/yano-0.1.0-pre17.zip`.
- Host ZIP SHA-256: `1a5cc5d188ad33b339a725e62e10175a7b06b3ae0c82c4d7f054a2200e713e8b`.
- Tested X ZIP SHA-256: `67617bc23239aa299e996985ca71f9cd638cf7e5b2e4713fe0c5bc1566c50b91`.
- Script: `.agents/skills/smoke-yano-x-release/scripts/smoke_release_zip.sh`
  with `--http-base 29770 --server-base 29370 --keep`.

| Gate | Result | Seconds |
|---|---|---:|
| Archive / layout and tool launchers | PASS / PASS | 5 / 4 |
| Packaged two-node runtime acceptance | PASS | 31 |
| Packaged three-node stock outcomes | PASS | 60 |
| Showcase doctor / quickstart / projection disabled | PASS / PASS / PASS | 0 / 95 / 0 |
| Showcase all scenarios / cross-node verification | PASS / PASS | 117 / 7 |
| Record tips / full restart / verification after restart | PASS / PASS / PASS | 0 / 56 / 6 |
| Persisted heights and roots / distribution config unchanged | PASS / PASS | 1 / 0 |

The script stopped its temporary nodes; HTTP 29770–29772 have no listeners.
The retained cluster on 7170–7172 was not changed. No preprod/mainnet traffic,
remote deployment, public artifact release, Docker evidence profile, or dedicated
eUTXO profile qualification is claimed. Existing opt-in/disabled source tests
remain skipped rather than being represented as executed.

### Final aggregate gate

The following incremental aggregate command passed in 2m45s (584 actionable
tasks: 63 executed, 521 up-to-date), after the preceding full source, integration,
and crypto runs and correction of all reported failures. This is not a claimed
`clean build`; distribution verification and packaged runtime smoke ran explicitly.

```bash
./gradlew build integrationTest cryptoTest distributionCheck \
  :tooling:devtools:distributionTest \
  :examples:showcase:showcaseScriptContract \
  :examples:showcase:showcaseDistributionContract \
  -PyanoVersion=0.1.0-pre17-SNAPSHOT \
  -PyanoJvmDist=/Users/satya/work/bloxbean/yano-adr031-1/app/build/distributions/yano-0.1.0-pre17.zip \
  -PuseMavenLocal=true -PskipSigning=true --offline --no-parallel --continue --console=plain
```

`build` includes artifact inventory and JVM-only boundary checks. The packaged
devtools suite passes four tests, including black-box binding compile/validate/
dry-run through the final ZIP launcher without integration-test dependencies.
All seven binding CLI integration cases and all four three-node recipe cases
pass. The twelve Studio Node tests and six governance plus four binding CDDL
vectors also pass (CDDL validator 0.10.5).

Upstream host gates: `:core-api:test :runtime:test :plugin-catalog:test
-PskipSigning=true --no-parallel --max-workers=2 --console=plain` passed in
10m08s; `publishToMavenLocal :app:yanoDistZip -PskipSigning=true --no-parallel`
passed for the exact inputs above. Remote CI must receive published/staged
artifacts from that host change; no repository CI inputs, release tags, retained
cluster identity, or signing material were changed to bypass that dependency.
